mode: one

scraper:
  base_url: "https://anarepo.kabucluster.com/?page={page}"
  sleep_range: [0.5, 1.2]

pages:
  start: 501
  end: 600

supabase:
  table: stock_reports
  batch_size: 500
  unique_keys:
    - code
    - report_date

output:
  save_csv: false
